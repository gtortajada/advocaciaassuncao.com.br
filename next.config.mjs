/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
{/* Não fiz a parte do tutorial do ChakraUi que altera este documento. O conteúdo que deveria estar aqui é: export default {
  experimental: {
    optimizePackageImports: ["@chakra-ui/react"],
  },
} */}